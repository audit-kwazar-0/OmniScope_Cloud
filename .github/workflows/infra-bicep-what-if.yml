# Subscription-level Bicep what-if using OIDC (no client secret).
# Expects GitHub Environment **bicep**:
#   Secrets: AZURE_CLIENT_ID, AZURE_TENANT_ID, AZURE_SUBSCRIPTION_ID (same as "Azure — connection test")
#   Variables (environment or repository): BICEP_PREFIX, BICEP_LOCATION; optional BICEP_DEPLOY_AKS / BICEP_DEPLOY_ACR
#   Secret or Variable: BICEP_ALERT_EMAIL
# Dispatch inputs can override AKS/ACR without changing permanent Variables.
#
# See infra/bicep/GITHUB_ACTIONS_VARIABLES.md (Environment + federated credential + pipeline chain).

name: 'Infra — Bicep what-if (Azure)'

on:
  workflow_dispatch:
    inputs:
      deploy_aks:
        description: 'deployAks parameter (use-variable = from BICEP_DEPLOY_AKS)'
        type: choice
        options:
          - use-variable
          - 'true'
          - 'false'
        default: use-variable
      deploy_acr:
        description: 'deployAcr parameter (use-variable = from BICEP_DEPLOY_ACR)'
        type: choice
        options:
          - use-variable
          - 'true'
          - 'false'
        default: use-variable

permissions:
  id-token: write
  contents: read

env:
  ARM_TEMPLATE: infra/bicep/main.bicep

jobs:
  what-if:
    runs-on: ubuntu-latest
    environment: bicep
    steps:
      - name: Git — default branch (quieter checkout logs)
        run: |
          git config --global init.defaultBranch main
          git config --global advice.defaultBranchName false

      - name: Checkout
        uses: actions/checkout@v4

      - name: Check required configuration
        env:
          SUB: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          TENANT: ${{ secrets.AZURE_TENANT_ID }}
          CLIENT: ${{ secrets.AZURE_CLIENT_ID }}
          PREFIX: ${{ vars.BICEP_PREFIX }}
          LOC: ${{ vars.BICEP_LOCATION }}
        run: |
          set -e
          for name in SUB TENANT CLIENT PREFIX LOC; do
            eval "v=\${$name}"
            if [ -z "$v" ]; then
              echo "Missing value for $name"
              echo "Azure: Repository or Environment **Secrets** AZURE_SUBSCRIPTION_ID, AZURE_TENANT_ID, AZURE_CLIENT_ID"
              echo "Bicep: **Variables** BICEP_PREFIX, BICEP_LOCATION (see \"Azure — connection test\" Preflight and GITHUB_ACTIONS_VARIABLES.md)"
              exit 1
            fi
          done
          echo "Required secrets/vars present."

      - name: Azure login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Bicep build
        run: az bicep build --file "$ARM_TEMPLATE"

      - name: Deployment what-if
        env:
          INPUT_DEPLOY_AKS: ${{ inputs.deploy_aks }}
          INPUT_DEPLOY_ACR: ${{ inputs.deploy_acr }}
          DEPLOY_AKS: ${{ vars.BICEP_DEPLOY_AKS }}
          DEPLOY_ACR: ${{ vars.BICEP_DEPLOY_ACR }}
          ALERT_SECRET: ${{ secrets.BICEP_ALERT_EMAIL }}
          ALERT_VAR: ${{ vars.BICEP_ALERT_EMAIL }}
        run: |
          set -euo pipefail
          ALERT="${ALERT_SECRET:-}"
          if [ -z "$ALERT" ]; then ALERT="${ALERT_VAR:-}"; fi
          if [ -z "$ALERT" ]; then
            echo "Set BICEP_ALERT_EMAIL as Environment or repository Variable or Secret."
            exit 1
          fi
          if [ "${INPUT_DEPLOY_AKS}" = "use-variable" ]; then
            AKS="${DEPLOY_AKS:-false}"
          else
            AKS="${INPUT_DEPLOY_AKS}"
          fi
          if [ "${INPUT_DEPLOY_ACR}" = "use-variable" ]; then
            ACR="${DEPLOY_ACR:-false}"
          else
            ACR="${INPUT_DEPLOY_ACR}"
          fi
          echo "what-if deployAks=$AKS deployAcr=$ACR (dispatch overrides use-variable → vars)"
          NAME="gh-whatif-${{ github.run_id }}-${{ github.run_attempt }}"
          az deployment sub what-if \
            --location "${{ vars.BICEP_LOCATION }}" \
            --name "$NAME" \
            --template-file "$ARM_TEMPLATE" \
            --no-pretty-print \
            --parameters \
              prefix="${{ vars.BICEP_PREFIX }}" \
              location="${{ vars.BICEP_LOCATION }}" \
              alertEmail="$ALERT" \
              deployAks="$AKS" \
              deployAcr="$ACR"
